package com.example.myapplication.domain

import org.junit.Assert.*

class BirthdateInteractorTest {

    fun yolo() {
        val mock = Birthdate(1, 1, 1)
        given(mock.is18YearsOld()).wil
    }
}